import { Container, Text } from '@/components/ui';

export const Footer = () => {
  return (
    <Container.Footer paddingY={'lg'} paddingX={'lg'} borderTop={'sm'}>
      <Text>Footer</Text>
    </Container.Footer>
  );
};
