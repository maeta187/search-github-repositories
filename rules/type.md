# 型

## 型定義

- 汎用的な型定義は`types`ディレクトリに配置する
- コンポーネント依存のものはコンポーネント内に配置する
  - 他の箇所でも必要になった場合は`types`ディレクトリに配置する
- コンポーネントのpropsは`interface`で定義する
- GitHub APIの返り値から参照するデータのはスネークケースになっている
  - APIの返り値の型定義の末尾には`Entity`を付ける
  - APIへのリクエストデータの型定義の末尾には`Dto`を付ける
